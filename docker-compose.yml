version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: prueba-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: prueba_db
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
